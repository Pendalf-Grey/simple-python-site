# Python Site

Простой одностраничный сайт на Python.

## Запуск

```bash
python3 app.py
```

Откройте `http://localhost:8000`.

## Docker

```bash
docker build -t python-site .
docker run --rm -p 8000:8000 python-site
```
